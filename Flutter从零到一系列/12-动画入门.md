# ✨ Flutter 从零到一（十二）：动画入门 — 让界面灵动起来

> **系列导读**：动画让 App 从"能用"变成"好用"。
> 本文从最简单的隐式动画开始，带你逐步掌握 Flutter 动画体系。

---

## 🎬 1. 隐式动画 — 改个值就有动画

```dart
class AnimatedDemo extends StatefulWidget {
  @override
  State<AnimatedDemo> createState() => _AnimatedDemoState();
}

class _AnimatedDemoState extends State<AnimatedDemo> {
  bool _expanded = false;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () => setState(() => _expanded = !_expanded),
      child: AnimatedContainer(
        duration: Duration(milliseconds: 300),
        curve: Curves.easeOutCubic,
        width: _expanded ? 280 : 140,
        height: _expanded ? 160 : 80,
        padding: EdgeInsets.all(_expanded ? 20 : 12),
        decoration: BoxDecoration(
          color: _expanded ? Colors.indigo : Colors.teal,
          borderRadius: BorderRadius.circular(_expanded ? 20 : 10),
        ),
        child: Center(
          child: Text(
            _expanded ? '点击收起' : '点击展开',
            style: TextStyle(color: Colors.white),
          ),
        ),
      ),
    );
  }
}
```

### 常用隐式动画

```dart
// 透明度渐变
AnimatedOpacity(
  opacity: _visible ? 1.0 : 0.0,
  duration: Duration(milliseconds: 300),
  child: Text('渐显文字'),
)

// 缩放
AnimatedScale(
  scale: _pressed ? 0.9 : 1.0,
  duration: Duration(milliseconds: 150),
  child: Card(child: Text('缩放卡片')),
)

// 内容切换（带过渡动画）
AnimatedSwitcher(
  duration: Duration(milliseconds: 300),
  child: Text(
    '$_count',
    key: ValueKey(_count),  // key 变化触发动画
    style: TextStyle(fontSize: 48),
  ),
)

// 交叉淡入淡出
AnimatedCrossFade(
  firstChild: Icon(Icons.favorite_border, size: 48),
  secondChild: Icon(Icons.favorite, size: 48, color: Colors.red),
  crossFadeState: _liked ? CrossFadeState.showSecond : CrossFadeState.showFirst,
  duration: Duration(milliseconds: 200),
)
```

---

## 🎡 2. 显式动画 — 完全掌控

```dart
class RotatingLogo extends StatefulWidget {
  @override
  State<RotatingLogo> createState() => _RotatingLogoState();
}

class _RotatingLogoState extends State<RotatingLogo>
    with SingleTickerProviderStateMixin {
  late final AnimationController _controller;

  @override
  void initState() {
    super.initState();
    _controller = AnimationController(
      duration: Duration(seconds: 2),
      vsync: this,
    )..repeat();  // 无限旋转
  }

  @override
  void dispose() {
    _controller.dispose();  // 必须释放！
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return RotationTransition(
      turns: _controller,
      child: FlutterLogo(size: 80),
    );
  }
}
```

---

## 🦸 3. Hero — 页面转场动画

```dart
// 列表页
Hero(
  tag: 'avatar-${user.id}',
  child: CircleAvatar(radius: 24, backgroundImage: NetworkImage(user.avatar)),
)

// 详情页（tag 必须一致）
Hero(
  tag: 'avatar-${user.id}',
  child: CircleAvatar(radius: 60, backgroundImage: NetworkImage(user.avatar)),
)
// Flutter 会自动在两个页面间实现飞行动画！
```

---

## 🎬 4. Lottie — 设计师级动画

```yaml
dependencies:
  lottie: ^3.1.0
```

```dart
// 从本地加载
Lottie.asset('assets/animations/loading.json', width: 200, height: 200)

// 从网络加载
Lottie.network('https://assets.lottiefiles.com/packages/lf20_success.json',
  width: 200, height: 200, repeat: false)
```

---

## ✅ 本篇小结 Checklist

- [ ] 掌握 AnimatedContainer / AnimatedOpacity 隐式动画
- [ ] 会用 AnimationController 做循环动画
- [ ] 理解 Hero 页面转场动画
- [ ] 会集成 Lottie 动画

---

> **下一篇预告**：《实战项目：从零搭建一个完整 App》

---

*本文是「Flutter 从零到一」系列第 12 篇，共 13 篇。*
