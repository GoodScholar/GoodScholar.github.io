# 🌱 RN 从零到一（六）：网络请求与数据处理 — 连接后端的桥梁

> **系列导读**：这是「RN 从零到一」系列的第 6 篇。几乎所有 App 都需要和服务端交互，
> 本篇将学习如何发起网络请求、处理响应数据，以及使用 TanStack Query 做数据缓存。

**本文目标**：掌握 fetch / axios 的用法，学会封装请求工具，了解 TanStack Query 数据缓存方案。

---

## 📊 请求方案对比

| 方案 | 特点 | 推荐度 |
|------|------|--------|
| fetch | 内置 API，无需安装 | ✅ 简单场景够用 |
| axios | 拦截器、取消请求、更丰富的 API | ⭐⭐⭐⭐⭐ 推荐 |
| TanStack Query | 数据缓存、自动刷新、加载/错误状态管理 | ⭐⭐⭐⭐⭐ 强烈推荐 |

---

## 🌐 1. fetch — 内置请求

```tsx
// GET 请求
const fetchUsers = async () => {
  try {
    const response = await fetch('https://api.example.com/users');
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('请求失败:', error);
    throw error;
  }
};

// POST 请求
const createUser = async (user: { name: string; email: string }) => {
  const response = await fetch('https://api.example.com/users', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
    },
    body: JSON.stringify(user),
  });
  return response.json();
};
```

---

## 📦 2. axios — 企业级请求库

### 安装

```bash
npm install axios
```

### 封装请求工具

```tsx
// utils/request.ts
import axios, { AxiosError, InternalAxiosRequestConfig } from 'axios';
import { useUserStore } from '../stores/useUserStore';
import { Alert } from 'react-native';

// 创建实例
const request = axios.create({
  baseURL: 'https://api.example.com',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
});

// 请求拦截器 — 自动携带 Token
request.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    const token = useUserStore.getState().token;
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器 — 统一错误处理
request.interceptors.response.use(
  (response) => {
    const { code, data, message } = response.data;
    if (code === 200) return data;
    // 业务错误
    Alert.alert('提示', message || '请求失败');
    return Promise.reject(new Error(message));
  },
  (error: AxiosError) => {
    if (error.response?.status === 401) {
      // Token 过期，跳转登录
      useUserStore.getState().logout();
    } else if (error.message.includes('timeout')) {
      Alert.alert('提示', '请求超时，请重试');
    } else {
      Alert.alert('提示', '网络异常，请检查网络连接');
    }
    return Promise.reject(error);
  }
);

export default request;
```

### 定义 API 模块

```tsx
// api/user.ts
import request from '../utils/request';

export interface User {
  id: string;
  name: string;
  avatar: string;
  email: string;
}

export const userApi = {
  // 获取用户列表
  getList: (params: { page: number; pageSize: number }) =>
    request.get<User[]>('/users', { params }),

  // 获取用户详情
  getDetail: (id: string) =>
    request.get<User>(`/users/${id}`),

  // 更新用户信息
  update: (id: string, data: Partial<User>) =>
    request.put(`/users/${id}`, data),

  // 删除用户
  delete: (id: string) =>
    request.delete(`/users/${id}`),
};
```

### 在组件中使用

```tsx
import { userApi, User } from '../api/user';

export default function UserListScreen() {
  const [users, setUsers] = useState<User[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadUsers();
  }, []);

  const loadUsers = async () => {
    try {
      setLoading(true);
      const data = await userApi.getList({ page: 1, pageSize: 20 });
      setUsers(data);
    } catch (error) {
      // 错误已在拦截器中处理
    } finally {
      setLoading(false);
    }
  };

  if (loading) return <ActivityIndicator size="large" />;

  return (
    <FlatList
      data={users}
      keyExtractor={item => item.id}
      renderItem={({ item }) => (
        <View style={styles.item}>
          <Image source={{ uri: item.avatar }} style={styles.avatar} />
          <Text>{item.name}</Text>
        </View>
      )}
    />
  );
}
```

---

## 🔄 3. TanStack Query — 数据缓存

TanStack Query（原 React Query）自动管理请求的加载、缓存、刷新和错误状态：

### 安装

```bash
npm install @tanstack/react-query
```

### 配置 Provider

```tsx
// app/_layout.tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,    // 5 分钟内数据视为新鲜
      retry: 2,                    // 失败重试 2 次
    },
  },
});

export default function RootLayout() {
  return (
    <QueryClientProvider client={queryClient}>
      <Stack />
    </QueryClientProvider>
  );
}
```

### 查询数据 — useQuery

```tsx
import { useQuery } from '@tanstack/react-query';
import { userApi } from '../api/user';

export default function UserListScreen() {
  const {
    data: users,
    isLoading,
    isError,
    error,
    refetch,
    isRefetching,
  } = useQuery({
    queryKey: ['users'],        // 缓存 key
    queryFn: () => userApi.getList({ page: 1, pageSize: 20 }),
  });

  if (isLoading) {
    return <ActivityIndicator size="large" />;
  }

  if (isError) {
    return (
      <View style={styles.error}>
        <Text>加载失败: {error.message}</Text>
        <Pressable onPress={() => refetch()}>
          <Text>重试</Text>
        </Pressable>
      </View>
    );
  }

  return (
    <FlatList
      data={users}
      keyExtractor={item => item.id}
      renderItem={({ item }) => <UserCard user={item} />}
      refreshControl={
        <RefreshControl refreshing={isRefetching} onRefresh={refetch} />
      }
    />
  );
}
```

### 修改数据 — useMutation

```tsx
import { useMutation, useQueryClient } from '@tanstack/react-query';

function CreateUserForm() {
  const queryClient = useQueryClient();

  const mutation = useMutation({
    mutationFn: (newUser: { name: string; email: string }) =>
      userApi.create(newUser),
    onSuccess: () => {
      // 创建成功后，刷新用户列表缓存
      queryClient.invalidateQueries({ queryKey: ['users'] });
      Alert.alert('成功', '用户创建成功');
    },
    onError: (error) => {
      Alert.alert('失败', error.message);
    },
  });

  const handleSubmit = () => {
    mutation.mutate({ name: '张三', email: 'zhangsan@example.com' });
  };

  return (
    <View>
      <Pressable onPress={handleSubmit} disabled={mutation.isPending}>
        <Text>{mutation.isPending ? '提交中...' : '创建用户'}</Text>
      </Pressable>
    </View>
  );
}
```

### 分页查询 — useInfiniteQuery

```tsx
import { useInfiniteQuery } from '@tanstack/react-query';

function InfiniteUserList() {
  const {
    data,
    fetchNextPage,
    hasNextPage,
    isFetchingNextPage,
    isLoading,
  } = useInfiniteQuery({
    queryKey: ['users', 'infinite'],
    queryFn: ({ pageParam = 1 }) =>
      userApi.getList({ page: pageParam, pageSize: 20 }),
    getNextPageParam: (lastPage, allPages) => {
      return lastPage.length === 20 ? allPages.length + 1 : undefined;
    },
    initialPageParam: 1,
  });

  const allUsers = data?.pages.flat() ?? [];

  return (
    <FlatList
      data={allUsers}
      keyExtractor={item => item.id}
      renderItem={({ item }) => <UserCard user={item} />}
      onEndReached={() => {
        if (hasNextPage) fetchNextPage();
      }}
      onEndReachedThreshold={0.3}
      ListFooterComponent={
        isFetchingNextPage ? <ActivityIndicator /> : null
      }
    />
  );
}
```

---

## 💡 数据流总结

```
组件 → useQuery / useMutation
         │
         ├── queryFn / mutationFn → API 模块（api/user.ts）
         │                              │
         │                              └── request 工具（utils/request.ts）
         │                                      ├── 请求拦截器（加 Token）
         │                                      ├── axios.create（baseURL、timeout）
         │                                      └── 响应拦截器（错误处理）
         │
         └── 自动管理：loading / error / data / cache / refetch
```

---

## ✅ 本篇小结 Checklist

- [ ] 掌握 fetch 基础请求用法
- [ ] 学会用 axios 封装请求工具（拦截器、错误处理）
- [ ] 按模块组织 API（api/user.ts 等）
- [ ] 掌握 TanStack Query 的 useQuery 和 useMutation
- [ ] 了解 useInfiniteQuery 实现无限滚动
- [ ] 理解完整的数据请求流转链路

---

> **下一篇预告**：《表单与用户输入》—— 学习表单处理、表单验证和键盘适配。

---

*本文是「RN 从零到一」系列第 6 篇，共 10 篇。*
